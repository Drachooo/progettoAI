\documentclass[a4paper, 12pt]{article} % Tipo di documento e dimensione font

% --- CODIFICA E LINGUA ---
\usepackage[utf8]{inputenc}  % Per scrivere accentate direttamente (à, è, ì...)
\usepackage[T1]{fontenc}     % Migliora la gestione dei font nel PDF
\usepackage[italian]{babel}  % Traduce le diciture automatiche (es. "Indice", "Figura")

% --- MATEMATICA E SCIENZE ---
\usepackage{amsmath}  % Il pacchetto base per la matematica
\usepackage{amssymb}  % Simboli matematici aggiuntivi
\usepackage{amsfonts} % Font matematici

% --- IMMAGINI E GRAFICA ---
\usepackage{graphicx} % Fondamentale per inserire immagini (\includegraphics)
\usepackage{float}    % Per forzare la posizione delle immagini (usando [H])

% --- LAYOUT E MARGINI ---
\usepackage{geometry} % Per modificare i margini
\geometry{a4paper, top=2.5cm, bottom=2.5cm, left=2.5cm, right=2.5cm}

% --- TABELLE ---
\usepackage{booktabs} % Per tabelle dall'aspetto professionale
\usepackage{caption}  % Per personalizzare le didascalie

% --- LINK E RIFERIMENTI (Caricare quasi sempre per ultimo) ---
\usepackage{hyperref} % Rende cliccabili l'indice e i riferimenti
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=magenta,      
    urlcolor=blue,
}

\begin{document}

% Qui inizia il tuo testo...
Documentazione rapida per il progetto di Intelligenza Artificiale

\end{document}